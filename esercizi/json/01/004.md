```sql

-- ===============================================
-- OPERATORI JSONB AVANZATI
-- ===============================================

-- Contenimento: @> (contiene)
SELECT * FROM products 
WHERE specifications @> '{"display": {"type": "OLED"}}';

-- Contenimento: <@ (è contenuto in)
SELECT * FROM products 
WHERE '{"fast_charging": true}' <@ specifications->'battery';

-- Sovrapposizione di chiavi
SELECT * FROM products 
WHERE specifications->'colors' && '["black", "red"]'::jsonb;

```